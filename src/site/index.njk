---
title: Adam Mish
layout: default
---

<div class="text-center">
  <p>I write <a href="/poems">poetry</a>,
    <a href="/code">code</a>,
    <a href="/blog">blog posts</a>,
    and <a href="https://twitter.com/adammish">tweets</a>.
  </p>
  <p>I publish a poem every day,</p>
  <p>and a blog post twice a week.</p>
  <p>Get in touch: <a href="mailto:mail@adammi.sh">mail@adammi.sh</a>.</p>
</div>
<div class="flex flex-col md:flex-row px-4">
  <div class="latest-poem md:w-1/2 py-6">
    <p class="italic">Today's poem:</p>
      {%- for item in collections.poem | head(-1) -%}
        <p>
          <a class="font-bold" href="{{ item.url }}">{{ item.data.title }}</a>
        </p>
        {{ item.templateContent | safe }}
      {%- endfor -%}
      <div class="mt-8">
        <a href="/poems">all poems</a>
      </div>
  </div>
  <div class="latest-posts md:w-1/2 py-6">
    <p class="italic">Latest posts:</p>
      {%- for item in collections.post | head(-3) | reverse -%}
        <p>
          <a class="font-bold" href="{{ item.url }}">{{ item.data.title }}</a>
        </p>
        {{ item.templateContent | section | safe }}
      {%- endfor -%}
    <p class="mt-8"><a href="/blog">all posts</a></p>
  </div>
  {# Music coming soon #}
  {# <div class="latest-track md:w-1/2 py-6">
    <p class="italic">Latest track:</p>
      {%- for item in collections.track | head(-1) -%}
        <p class="font-bold">
          <a href="{{ item.url }}">{{ item.data.title }}</a>
        </p>
        {{ item.templateContent | section | safe }}
      {%- endfor -%}
    <div class="mt-8">
    <a href="/blog">all tracks</a> &bull; 
        <a href="/poems">spotify</a>
      </div>
  </div> #}
</div>
<div class="newsletter text-center md:w-1/2 m-auto py-6 px-4">
  <p class="italic py-2">Get an email each Friday with the weeks new writings.</p>
  <script async data-uid="03d2b6e1f7" src="https://sunny-leader-1529.ck.page/03d2b6e1f7/index.js"></script>
  <p class="italic">No spam, unsubscribe anytime.</p>
</div>
